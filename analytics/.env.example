# Local ClickHouse — defaults match the auto-launched container that
# `envio dev` spins up when storage.clickhouse: true.
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=testing
CLICKHOUSE_DATABASE=envio_sink
